const leitor = require('readline-sync')
class professor{
     constructor(nome:string, idade:number, ano_exp:number)

}
let menu = boolean
let prof = new professor
whle(menu){
    let opcao = leitor.questionInt('Insira o número da opção desejada:\n 1 - Criar Professor \n 2 - Criar Escola  ')
}